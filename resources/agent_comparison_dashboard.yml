resources:
  dashboards:
    agent_comparison_dashboard:
      display_name: "[${bundle.target}] Red Bricks Insurance — Agent A/B Comparison"
      warehouse_id: ${var.warehouse_id}
      file_path: ../src/dashboards/agent_comparison_dashboard.lvdash.json
      permissions:
        - level: CAN_READ
          group_name: users
