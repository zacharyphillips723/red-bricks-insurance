resources:
  dashboards:
    care_management_dashboard:
      display_name: "[${bundle.target}] Red Bricks Insurance — Care Management Operations"
      warehouse_id: ${var.warehouse_id}
      file_path: ../src/dashboards/care_management_dashboard.lvdash.json
      permissions:
        - level: CAN_READ
          group_name: users
