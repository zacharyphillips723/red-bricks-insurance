resources:
  dashboards:
    observability_dashboard:
      display_name: "[${bundle.target}] Red Bricks Insurance — Agent Observability"
      warehouse_id: ${var.warehouse_id}
      file_path: ../src/dashboards/observability_dashboard.lvdash.json
      permissions:
        - level: CAN_READ
          group_name: users
