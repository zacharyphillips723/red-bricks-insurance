resources:
  dashboards:
    pa_operations_dashboard:
      display_name: "[${bundle.target}] Red Bricks Insurance — PA Operations"
      warehouse_id: ${var.warehouse_id}
      file_path: ../src/dashboards/pa_operations_dashboard.lvdash.json
      permissions:
        - level: CAN_READ
          group_name: users
