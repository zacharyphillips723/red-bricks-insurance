resources:
  dashboards:
    red_bricks_analytics_dashboard:
      display_name: "[${bundle.target}] Red Bricks Insurance — Analytics Dashboard"
      warehouse_id: ${var.warehouse_id}
      file_path: ../src/dashboards/red_bricks_dashboard.lvdash.json
      permissions:
        - level: CAN_READ
          group_name: users
